# MongoDB migration / future runtime switch
MONGODB_URI=mongodb://127.0.0.1:27017
MONGODB_DB=my_trpg_app
USE_MONGODB=false
# items source: false=json, true=mongodb, auto=use mongodb only on Render
USE_MONGO_ITEMS=true
